# a simple python script that queryies image metadata and finds a common-denominator baseline that will be used to normalize the images and randomize the filenames
# the baseline will be determined via LLM using Foundry Local

