
# Ignore rembg-specific cache (not in parent)
rembg_cache/

# Ignore Tkinter window state (if any, not in parent)
*.tkstate

# Ignore OS-specific files (not in parent)
.DS_Store
Thumbs.db


# Ignore output images only in output/ folder (customize as needed)
output/
output/*.png
output/*.jpg
output/*.jpeg
output/*.bmp
output/*.webp

# Allow images in assets/ and docs/
!assets/
!assets/*.png
!assets/*.jpg
!assets/*.jpeg
!assets/*.bmp
!assets/*.webp
!docs/
!docs/*.png
!docs/*.jpg
!docs/*.jpeg
!docs/*.bmp
!docs/*.webp

# Ignore user-specific files (not in parent)
*.swp
*.swo
